version: '3'
services:
  mongodb:
    image: mongo
    volumes:
      - ~/data/db:/data/db
    ports:
      - 27017:27017

  elasticsearch:
    image: elasticsearch

  kibana:
    image: kibana
    ports:
      - 5601:5601